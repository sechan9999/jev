# Local JEV runtime.
#
# sglang[all] pulls in the inference server plus its model/runtime deps and
# needs a CUDA GPU to serve models. requests is the only thing the client
# scripts (decide.py, benchmark.py) actually import.
sglang[all]==0.5.10.post1
requests==2.32.3
