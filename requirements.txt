# Client + app deps (no GPU). This is what Streamlit Community Cloud installs
# from the repo root, and all decide.py / benchmark.py / app.py need.
# The SGLang server has its own heavier deps in requirements-server.txt.
streamlit>=1.36,<2
requests>=2.31,<3
